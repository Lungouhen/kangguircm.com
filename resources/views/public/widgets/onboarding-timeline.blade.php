@php $items=$steps??[['title'=>'Discovery','description'=>'Goals, payer mix, systems, and current performance.'],['title'=>'Workflow assessment','description'=>'Document current-state processes and opportunities.'],['title'=>'Secure setup','description'=>'Access, integrations, controls, and team alignment.'],['title'=>'Validation','description'=>'Test workflows and reconcile output before launch.'],['title'=>'Go-live','description'=>'Transition with clear ownership and issue escalation.'],['title'=>'Optimization','description'=>'Review KPIs and continuously improve performance.']]; @endphp<section class="py-20 bg-white"><div class="max-w-7xl mx-auto px-6"><div class="max-w-3xl"><h2 class="text-4xl font-bold text-slate-900">{{ $title??'A clear path from discovery to measurable improvement' }}</h2>@if(!empty($description))<p class="mt-4 text-slate-600">{{ $description }}</p>@endif</div><ol class="mt-12 grid md:grid-cols-3 gap-6">@foreach($items as $step)<li class="rounded-2xl border border-slate-200 p-6"><span class="text-blue-600 font-bold">Step {{ $loop->iteration }}</span><h3 class="mt-3 text-xl font-bold">{{ $step['title']??'' }}</h3><p class="mt-2 text-slate-600">{{ $step['description']??'' }}</p></li>@endforeach</ol></div></section>
