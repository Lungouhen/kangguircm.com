<section class="p-8 rounded shadow {{ !empty($highlighted) ? 'ring-2 ring-blue-600' : '' }}"><h3 class="text-2xl font-bold">{{ $title ?? '' }}</h3><p class="text-4xl my-4">{{ $price ?? '' }}</p><ul>@foreach(preg_split('/\r?\n/', $features ?? '') as $feature) @if($feature)<li>✓ {{ $feature }}</li>@endif @endforeach</ul>@if(!empty($cta_text))<a class="inline-block mt-6 bg-blue-600 text-white px-5 py-2" href="{{ $cta_url ?? '#' }}">{{ $cta_text }}</a>@endif</section>
