<section class="py-12 text-center bg-blue-600 text-white" @if(!empty($background_image)) style="background-image:url('{{ $background_image }}')" @endif><h2 class="text-3xl font-bold">{{ $title ?? '' }}</h2><p class="my-4">{{ $description ?? '' }}</p>@if(!empty($button_text))<a class="inline-block bg-white text-blue-600 px-6 py-3 rounded" href="{{ $button_url ?? '#' }}">{{ $button_text }}</a>@endif</section>
