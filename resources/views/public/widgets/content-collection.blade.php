<section class="py-16"><div class="max-w-7xl mx-auto px-6">@if(!empty($data['title']))<h2 class="text-3xl font-bold">{{ $data['title'] }}</h2>@endif<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">@foreach($entries as $entry)<article class="card p-6">@if($entry->image)<x-responsive-image :src="$entry->image" :alt="$entry->title" width="600" height="400" class="rounded-lg mb-5" />@endif<h3 class="text-xl font-bold">{{ $entry->title }}</h3><p class="mt-3 text-slate-600">{{ $entry->summary }}</p><a class="mt-5 inline-flex font-semibold text-blue-600" href="{{ route('content.show',[$entry->type,$entry->slug]) }}">View {{ $entry->title }}</a></article>@endforeach</div></div></section>