@php $items=$integrations??[]; @endphp<section class="py-16 bg-slate-50"><div class="max-w-7xl mx-auto px-6 text-center"><h2 class="text-3xl font-bold text-slate-900">{{ $title??'Designed to work with your healthcare technology stack' }}</h2>@if(!empty($description))<p class="mt-4 text-slate-600">{{ $description }}</p>@endif@if(count($items))<div class="mt-10 grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">@foreach($items as $item)<div class="bg-white border rounded-xl min-h-28 p-4 flex flex-col items-center justify-center">@if(!empty($item['logo']))<img src="{{ $item['logo'] }}" alt="{{ $item['name']??'' }}" width="180" height="60" loading="lazy" decoding="async" class="max-h-10 max-w-full">@else<strong>{{ $item['name']??'' }}</strong>@endif<small class="mt-2 text-slate-500">{{ $item['category']??'' }}</small></div>@endforeach</div>@endif<p class="mt-6 text-xs text-slate-500">{{ $footnote??'Product names and trademarks belong to their respective owners. Compatibility should be validated during discovery.' }}</p></div></section>
