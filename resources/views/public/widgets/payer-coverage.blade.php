@php $items = $payers ?? []; @endphp
<section class="py-16 bg-white"><div class="max-w-7xl mx-auto px-6 text-center"><h2 class="text-3xl font-bold text-slate-900">{{ $title ?? 'Experience across leading payer networks' }}</h2>@if(!empty($description))<p class="mt-3 text-slate-600 max-w-2xl mx-auto">{{ $description }}</p>@endif@if(count($items))<div class="mt-10 grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">@foreach($items as $payer)<div class="min-h-24 rounded-xl border border-slate-200 flex items-center justify-center p-4">@if(!empty($payer['logo']))<img width="180" height="60" loading="lazy" decoding="async" class="max-h-10 max-w-full" src="{{ $payer['logo'] }}" alt="{{ $payer['name'] ?? 'Payer' }} logo">@else<span class="font-semibold text-slate-600">{{ $payer['name'] ?? '' }}</span>@endif</div>@endforeach</div>@endif@if(!empty($footnote))<p class="mt-6 text-xs text-slate-400">{{ $footnote }}</p>@endif</div></section>
