@extends('layouts.app')
@section('seo')<x-seo-meta :title="$entry->meta_title?:$entry->title" :description="$entry->meta_description?:$entry->summary" :url="route('content.show',[$entry->type,$entry->slug])" :image="$entry->image" />@endsection
@section('content')<article class="py-16"><div class="max-w-4xl mx-auto px-6"><p class="uppercase text-sm font-bold text-blue-600">{{ str($entry->type)->headline() }}</p><h1 class="text-4xl font-bold mt-3">{{ $entry->title }}</h1>@if($entry->image)<x-responsive-image :src="$entry->image" :alt="$entry->title" width="1200" height="630" class="mt-8 rounded-2xl" :priority="true" />@endif<p class="mt-6 text-xl text-slate-600">{{ $entry->summary }}</p><div class="mt-8 whitespace-pre-wrap leading-7">{{ $entry->body }}</div></div></article>@endsection