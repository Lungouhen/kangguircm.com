@extends('layouts.app')
@section('seo')<x-seo-meta :title="'Search results for '.$query" description="Search results" :url="request()->url()" />@endsection
@section('content')<section class="py-16"><div class="max-w-5xl mx-auto px-6"><h1 class="text-4xl font-bold">Search results for “{{ $query }}”</h1><form action="{{ route('search') }}" class="mt-6 flex gap-2"><input class="form__input" name="q" value="{{ $query }}"><button class="btn btn--primary">Search</button></form><div class="mt-10 space-y-8">@foreach($pages as $item)<article><h2 class="text-xl font-bold"><a href="{{ route('page.show',$item->slug) }}">{{ $item->title }}</a></h2><p>{{ $item->meta_description }}</p></article>@endforeach @foreach($posts as $item)<article><h2 class="text-xl font-bold"><a href="{{ route('post.show',$item->slug) }}">{{ $item->title }}</a></h2><p>{{ $item->excerpt }}</p></article>@endforeach @foreach($entries as $item)<article><h2 class="text-xl font-bold"><a href="{{ route('content.show',[$item->type,$item->slug]) }}">{{ $item->title }}</a></h2><p>{{ $item->summary }}</p></article>@endforeach @if($pages->isEmpty()&&$posts->isEmpty()&&$entries->isEmpty())<p>No results found.</p>@endif</div></div></section>@endsection