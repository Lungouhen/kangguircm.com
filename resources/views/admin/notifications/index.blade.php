@extends('layouts.admin')
@section('title','Notification Deliveries')
@section('content')
<div class="p-6"><h1 class="text-2xl font-bold">Notification deliveries</h1><p class="text-slate-500">Email and WhatsApp delivery status. Recipient values are masked.</p>@if(session('success'))<div class="alert alert--success mt-5">{{ session('success') }}</div>@endif<form class="flex gap-3 my-6"><select name="channel" class="form__select max-w-48"><option value="">All channels</option><option @selected(request('channel')==='email')>email</option><option @selected(request('channel')==='whatsapp')>whatsapp</option></select><select name="status" class="form__select max-w-48"><option value="">All statuses</option>@foreach(['pending','sent','delivered','read','failed'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="btn btn--primary">Filter</button></form><div class="card overflow-x-auto"><table><thead><tr><th>Created</th><th>Event</th><th>Channel</th><th>Recipient</th><th>Status</th><th>Attempts</th><th>Failure</th><th></th></tr></thead><tbody>@forelse($deliveries as $delivery)<tr><td>{{ $delivery->created_at->format('M j H:i') }}</td><td>{{ $delivery->event }}</td><td>{{ $delivery->channel }}</td><td>{{ $delivery->recipient_masked }}</td><td><span class="badge {{ $delivery->status==='failed'?'badge--danger':'badge--success' }}">{{ $delivery->status }}</span></td><td>{{ $delivery->attempts }}</td><td>{{ $delivery->failure_code?:'—' }}</td><td>@if($delivery->status==='failed')<form method="POST" action="{{ route('admin.notifications.retry',$delivery) }}">@csrf<button class="text-blue-600">Retry</button></form>@endif</td></tr>@empty<tr><td colspan="8">No notification attempts.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $deliveries->links() }}</div></div>
@endsection
