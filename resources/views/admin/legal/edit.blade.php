@extends('layouts.admin')
@section('title','Edit Policy')
@section('content')<div class="p-6 max-w-4xl"><h1 class="text-2xl font-bold">{{ $policy->title }}</h1><form method="POST" action="{{ route('admin.legal.update',$policy) }}" class="card p-6 mt-6 space-y-4">@csrf @method('PUT')<input class="form__input" name="title" value="{{ $policy->title }}"><div class="grid md:grid-cols-3 gap-3"><select class="form__select" name="type">@foreach(\App\Models\LegalPolicy::TYPES as $type)<option @selected($policy->type===$type)>{{ $type }}</option>@endforeach</select><input class="form__input" name="version" value="{{ $policy->version }}"><input class="form__input" type="date" name="effective_at" value="{{ $policy->effective_at->toDateString() }}"></div><input class="form__input" name="slug" value="{{ $policy->slug }}"><textarea class="form__textarea min-h-96" name="content">{{ $policy->content }}</textarea><label><input type="checkbox" name="is_published" value="1" @checked($policy->is_published)> Published</label><label><input type="checkbox" name="show_in_footer" value="1" @checked($policy->show_in_footer)> Show in footer</label><button class="btn btn--primary">Save policy</button></form></div>@endsection