@extends('layouts.admin')
@section('content')
<div class="p-6"><div class="flex justify-between items-center"><div><h1 class="text-2xl font-bold">Marketing leads</h1><p class="text-slate-500">RCM assessments and website inquiries</p></div><form><select name="status" onchange="this.form.submit()" class="rounded border-slate-300"><option value="">All statuses</option>@foreach(['new','contacted','qualified','proposal','won','lost'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></form></div><div class="grid sm:grid-cols-4 gap-4 my-6">@foreach($summary as $label=>$value)<div class="bg-white rounded-xl shadow p-5"><div class="text-sm text-slate-500">{{ str($label)->headline() }}</div><strong class="text-3xl">{{ $value }}</strong></div>@endforeach</div><div class="bg-white rounded-xl shadow overflow-x-auto"><table class="w-full"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Lead</th><th class="p-4 text-left">Practice</th><th class="p-4 text-left">Specialty</th><th class="p-4 text-left">Source</th><th class="p-4 text-left">Status</th><th></th></tr></thead><tbody>@forelse($leads as $lead)<tr class="border-t"><td class="p-4"><strong>{{ $lead->name }}</strong><br><span class="text-sm text-slate-500">{{ $lead->email }}</span></td><td class="p-4">{{ $lead->organization ?: '—' }}</td><td class="p-4">{{ $lead->specialty ?: '—' }}</td><td class="p-4">{{ $lead->utm_source ?: $lead->source }}</td><td class="p-4"><span class="rounded-full bg-blue-50 text-blue-700 px-3 py-1 text-sm">{{ $lead->status }}</span></td><td class="p-4"><a class="text-blue-600" href="{{ route('admin.marketing-leads.show',$lead) }}">View</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center">No leads found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $leads->links() }}</div></div>
@endsection
