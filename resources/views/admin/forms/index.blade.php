@extends('layouts.admin')
@section('title','Forms')
@section('content')<div class="p-6"><h1 class="text-2xl font-bold">No-code forms</h1><p class="text-slate-500">Build validated public forms and attach them to any page with the Form widget.</p><form method="POST" action="{{ route('admin.forms.store') }}" class="card p-5 mt-6 flex flex-wrap gap-3">@csrf<input class="form__input max-w-sm" name="name" placeholder="Form name" required><select class="form__select max-w-48" name="purpose"><option value="lead">Lead</option><option value="contact">Contact</option><option value="general">General</option></select><button class="btn btn--primary">Create form</button></form><div class="card mt-6 overflow-x-auto"><table><thead><tr><th>ID</th><th>Form</th><th>Purpose</th><th>Fields</th><th>Submissions</th><th>Status</th><th></th></tr></thead><tbody>@foreach($forms as $form)<tr><td>{{ $form->id }}</td><td>{{ $form->name }}<div class="text-xs text-slate-500">{{ $form->slug }}</div></td><td>{{ $form->purpose }}</td><td>{{ $form->fields_count }}</td><td><a href="{{ route('admin.forms.submissions',$form) }}">{{ $form->submissions_count }}</a></td><td>{{ $form->is_active?'Active':'Disabled' }}</td><td><a class="text-blue-600" href="{{ route('admin.forms.edit',$form) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $forms->links() }}</div></div>@endsection
