@extends('layouts.admin')
@section('title','Media Library')
@section('content')
<div class="p-6"><h1 class="text-2xl font-bold">Media library</h1><p class="text-slate-500">Upload reusable images and documents. SVG and executable formats are blocked.</p>@if(session('success'))<div class="alert alert--success mt-5">{{ session('success') }}</div>@endif<form method="POST" enctype="multipart/form-data" action="{{ route('admin.media.store') }}" class="card mt-6"><div class="card__body grid md:grid-cols-[1fr_1fr_auto] gap-4 items-end">@csrf<label><span class="form__label">File</span><input class="form__file" type="file" name="file" accept="image/jpeg,image/png,image/webp,image/avif,image/gif,application/pdf" required></label><label><span class="form__label">Alternative text</span><input class="form__input" name="alt_text" maxlength="255"></label><button class="btn btn--primary">Upload</button></div></form><div class="mt-6 grid grid-cols-2 md:grid-cols-4 xl:grid-cols-6 gap-4">@foreach($media as $item)<article class="card"><div class="aspect-square bg-slate-100 flex items-center justify-center overflow-hidden">@if($item->isImage())<img src="{{ $item->url }}" alt="{{ $item->alt_text }}" loading="lazy" class="w-full h-full object-cover">@else<img src="{{ asset($item->mime_type==='application/pdf'?'images/file-pdf.svg':'images/file-generic.svg') }}" alt="{{ $item->mime_type==='application/pdf'?'PDF document':'Document' }}" width="120" height="150" class="h-28 w-auto">@endif</div><div class="p-3"><p class="text-xs truncate" title="{{ $item->original_filename }}">{{ $item->original_filename }}</p><input class="mt-2 w-full text-xs" readonly value="{{ $item->url }}" aria-label="Media URL"><form method="POST" action="{{ route('admin.media.destroy',$item) }}" class="mt-2">@csrf @method('DELETE')<button class="text-xs text-red-600">Delete</button></form></div></article>@endforeach</div><div class="mt-6">{{ $media->links() }}</div></div>
@endsection
