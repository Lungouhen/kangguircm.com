@extends('layouts.admin')
@section('title','Structured Content')
@section('content')<div class="p-6"><div class="flex justify-between"><div><h1 class="text-2xl font-bold">Structured RCM content</h1><p class="text-slate-500">Reusable records update every page that displays this content type.</p></div><a class="btn btn--primary" href="{{ route('admin.content.create',['type'=>$type]) }}">Create {{ str($type)->headline() }}</a></div><nav class="flex flex-wrap gap-2 my-6">@foreach($types as $item)<a class="px-3 py-2 rounded {{ $type===$item?'bg-slate-900 text-white':'bg-white' }}" href="{{ route('admin.content.index',['type'=>$item]) }}">{{ str($item)->headline() }}</a>@endforeach</nav><div class="card overflow-x-auto"><table><thead><tr><th>Title</th><th>Status</th><th>Order</th><th>Updated</th><th></th></tr></thead><tbody>@foreach($entries as $entry)<tr><td>{{ $entry->title }}</td><td>{{ $entry->status }}</td><td>{{ $entry->sort_order }}</td><td>{{ $entry->updated_at->diffForHumans() }}</td><td><a href="{{ route('admin.content.edit',$entry) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $entries->links() }}</div></div>@endsection