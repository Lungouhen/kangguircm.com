@extends('layouts.admin')
@section('content')<div class="p-6"><h1 class="text-2xl font-bold mb-4">Edit role</h1><form method="POST" action="{{ route('admin.roles.update', $role) }}">@csrf @method('PUT')<input class="block border p-2 mb-3" name="name" value="{{ old('name', $role->name) }}" required><input class="block border p-2 mb-3" name="display_name" value="{{ old('display_name', $role->display_name) }}" required><textarea class="block border p-2 mb-3" name="description">{{ old('description', $role->description) }}</textarea>@foreach($groupedPermissions as $group => $permissions)<fieldset><legend>{{ $group }}</legend>@foreach($permissions as $permission)<label class="block"><input type="checkbox" name="permissions[]" value="{{ $permission->id }}" @checked(in_array($permission->id, $rolePermissions))> {{ $permission->display_name }}</label>@endforeach</fieldset>@endforeach<button class="mt-4 px-4 py-2 bg-blue-600 text-white">Save</button></form></div>@endsection
