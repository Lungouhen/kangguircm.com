<form method="POST" action="{{ $list ? route('admin.email.lists.update', $list) : route('admin.email.lists.store') }}">@csrf @if($list) @method('PUT') @endif<input class="block border p-2 mb-3 w-full" name="name" value="{{ old('name', $list?->name) }}" required><textarea class="block border p-2 mb-3 w-full" name="description">{{ old('description', $list?->description) }}</textarea><button class="px-4 py-2 bg-blue-600 text-white">Save</button></form>
