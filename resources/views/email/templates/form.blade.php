<form method="POST" action="{{ $template ? route('admin.email.templates.update', $template) : route('admin.email.templates.store') }}">@csrf @if($template) @method('PUT') @endif<input class="block border p-2 mb-3 w-full" name="name" value="{{ old('name', $template?->name) }}" required><input class="block border p-2 mb-3 w-full" name="subject" value="{{ old('subject', $template?->subject) }}" required><textarea class="block border p-2 mb-3 w-full" name="html_content" rows="12" required>{{ old('html_content', $template?->html_content) }}</textarea><textarea class="block border p-2 mb-3 w-full" name="text_content" rows="5">{{ old('text_content', $template?->text_content) }}</textarea><button class="px-4 py-2 bg-blue-600 text-white">Save</button></form>
